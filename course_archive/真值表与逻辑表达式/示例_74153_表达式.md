# 74153 单芯片示例

4 选 1 多路选择器：`M = MUX4(B, A, C0, C1, C2, C3)`。

- 选择位：`B`（高位），`A`（低位）
- 选择索引：`sel = 2*B + A`
- 输出：`M = C_sel`
- 默认使能：`1G̅ = 0`（由综合器自动连到 GND）

```txt
chips:   74153 x 1
inputs:  B, A, C0, C1, C2, C3
outputs: M

M = MUX4(B, A, C0, C1, C2, C3)
```
